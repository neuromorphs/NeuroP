"""Base-classes for models that can be derived from QUBO form.

.. autoclass:: neurop.Model.Model.Model
"""

import numpy as np
from collections.abc import Iterable

class Model(object):
    """Base-class for models that can be derived from QUBO form."""
    def __init__(object):
        pass